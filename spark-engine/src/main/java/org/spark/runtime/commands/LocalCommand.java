package org.spark.runtime.commands;

/**
 * Local commands can be sent only locally on the same machine
 * @author Monad
 *
 */
@SuppressWarnings("serial")
abstract public class LocalCommand extends ModelManagerCommand {
}
